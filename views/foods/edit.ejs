<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit <%= food.name %></title>
  </head>
  <body>
    <body>
        <!-- update the header -->
        <h1>Edit <%= food.name %></h1>
        <form action="/foods/<%=food._id%>?_method=PUT" method="POST">
            <div>
                <label for="name">Name:</label>
                <input type="text" name="name" id="name" value="<%= food.name %>" required>
            </div> <br/>
    
            <div>
                <label for="description">Describe it:</label>
                <textarea name="description" id="description" value="<%= food.description %>" required></textarea>
            </div> <br/>
    
            <div>
                <label for="image">Image:</label>
                <input type="text" name="image" id="image" value="<%= food.image %>" required>
            </div>

            <button type="submit">Update Food</button>
          </form>
        <a href="/foods">Back to Food</a>
      </body>
  </body>
</html>